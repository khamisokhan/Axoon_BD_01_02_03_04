<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit form</title>
</head>
<body>
    <h2>Edit Your Chat Msg ID: <%= foundChat._id %></h2>
    <p>This msg was sent From <b><%= foundChat.from %></b> to <b><%= foundChat.to %></b></p><br>
    <form method="POST" action="/chats/<%= foundChat._id %>?_method=PUT">
        <textarea name="msg" placeholder="Edit you msg" cols="20" rows="5"><%= foundChat.msg%></textarea><br>
        <button>Edit</button>
    </form>

    
</body>
</html>